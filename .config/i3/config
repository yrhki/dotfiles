# Generated with i3-config !!! No point in editing this file
# Files:
# .config/i3/config.d/00-base.i3.config
# .config/i3/config.d/10-variables.i3.config
# .config/i3/config.d/10-workspace.i3.config
# .config/i3/config.d/autostart.i3.config
# .config/i3/config.d/bar.i3.config
# .config/i3/config.d/binds.i3.config
# .config/i3/config.d/games.i3.config
# .config/i3/config.d/modes.i3.config
# .config/i3/config.d/window.i3.config
# .config/i3/config.d/window-x11.i3.config
# .config/i3/config.d/window-xwayland.i3.config
# .config/i3/config.d/wine.i3.config

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod


# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

set $left h
set $down j
set $up k
set $right l


set $monitor_primary "DisplayPort-0"
set $monitor_left "HDMI-A-0"
set $monitor_right "DisplayPort-1"
set $workspace_steam "9: STEAM"
set $workspace_rpcs "9: RPCS"
set $workspace_game "10: GAME"
set $workspace_private "9: PRIVATE"


workspace 1 output $monitor_primary 
workspace 2 output $monitor_right
workspace 3 output $monitor_left

# Assign workspaces to outputs
workspace $workspace_steam output $monitor_primary
workspace $workspace_game output $monitor_primary
workspace $workspace_private output $monitor_left

#exec --no-startup-id /usr/lib/xdg-desktop-portal-gtk
exec --no-startup-id /usr/lib/xdg-desktop-portal-kde -r
exec --no-startup-id /usr/lib/xdg-desktop-portal

# KDE Connect
exec --no-startup-id /usr/lib/kdeconnectd

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# start compositor
exec --no-startup-id picom -b --blur-background

# cosair keyboard light
#exec --no-startup-id ckb-next -b

# keyboard layout
exec --no-startup-id setxkbmap -layout yrski/programming

bar {
        status_command i3status
        tray_output primary
}


# Audio control
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym Shift+XF86AudioMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym Ctrl+XF86AudioMute exec --no-startup-id qdbus org.mpris.MediaPlayer2.plasma-browser-integration /org/mpris/MediaPlayer2 PlayPause

# start a terminal
bindsym $mod+Return exec alacritty
#bindsym $mod+Return exec konsole --profile i3 --hide-menubar

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# Moving around {{{ #

# Move your focus around
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right
# Or use $mod+[up|down|left|right]
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move the focused window with the same, but add Shift
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right
# Ditto, with arrow keys
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# }}} Moving around #

# Layout {{{ #
# You can "split" the current object of your focus with
# $mod+b or $mod+v, for horizontal and vertical splits
# respectively.
bindsym $mod+b splith
bindsym $mod+v splitv

# Switch the current container between different layout styles
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Make the current focus fullscreen
bindsym $mod+f fullscreen

# Toggle the current focus between tiling and floating mode
bindsym $mod+Shift+space floating toggle

# Swap focus between the tiling area and the floating area
bindsym $mod+space focus mode_toggle

# Move focus to the parent container
bindsym $mod+a focus parent

# }}} Layout #

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10



# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec i3-msg exit

# screenshot capture
bindsym Print exec spectacle

# boomer
#bindsym $mod+z exec boomer -w 
bindsym $mod+z exec boomer
bindsym $mod+Shift+z exec pkill boomer
# Steam
# https://github.com/ValveSoftware/steam-for-linux/issues/1040
for_window [class="^Steam$" title="^Friends$"] floating enable
for_window [class="^Steam$" title="Steam - News"] floating enable
for_window [class="^Steam$" title=".* - Chat"] floating enable
for_window [class="^Steam$" title="^Settings$"] floating enable
for_window [class="^Steam$" title=".* - event started"] floating enable
for_window [class="^Steam$" title=".* CD key"] floating enable
for_window [class="^Steam$" title="^Steam - Self Updater$"] floating enable
for_window [class="^Steam$" title="^Screenshot Uploader$"] floating enable
for_window [class="^Steam$" title="^Steam Guard - Computer Authorization Required$"] floating enable
for_window [title="^Steam Keyboard$"] floating enable

assign [class="^Steam$"] $workspace_steam

# steam games using proton
assign [class="^steam_app_.*"] $workspace_game

# steam native games
set $steam_native "^FTL.amd64$|^hl2_linux$"
for_window [class=$steam_native] move container to workspace $workspace_game, fullscreen


# rpcs3
assign [class="^RPCS3$" title="^RPCS3 .*"] $workspace_rpcs
assign [class="^RPCS3$" title="FPS"] $workspace_game
#assign [class="^rpcs$"] $workspace_game

bindsym $mod+r mode "resize"

# Resizing containers {{{ #
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    bindsym M resize set width 1920px height 1080px; move position 0 0

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
# }}} Resizing containers #
assign [class="^Boomer$"] $ws1
for_window [class="^Boomer$"] focus
#for_window [app_id="firefox.*" title="Launch Application"] floating enable
#for_window [app_id="firefox.*" title="Picture-in-Picture"] floating enable
#for_window [app_id="firefox.*" title="Firefox Developer Edition - Sharing Indicator"] floating enable
#for_window [app_id="io.crow_translate.CrowTranslate"] floating enable
#for_window [app_id="firefox.*" title=""] floating enable

assign [class="KeePassXC"] $workspace_private
for_window [class="KeePassXC" title="KeePassXC - Browser Access Request"] floating enable

# Carla plugins
for_window [class="zenity"] floating enable

for_window [class="MuPDF"] floating enable

# plasma/KDE {{{ #
#for_window [app_id="org.kde.yakuake"] floating enable
for_window [class="dolphin"] floating enable
#for_window [app_id="xdg-desktop-portal-kde"] floating enable
#for_window [app_id="org.kde.kdeconnect.daemon"] floating enable; sticky enable
#for_window [app_id="org.kde.kdeconnect-settings"] floating enable
#for_window [app_id="org.kde.keditfiletype"] floating enable
#for_window [app_id="org.kde.ark" title="^Compressing.*"] floating enable

for_window [class="kdenlive" title=".*P&roject Monitor.*"] border normal
for_window [title="Desktop — Plasma"] kill, floating enable, border none
for_window [class="plasmashell"] floating enable
for_window [class="Plasma"] floating enable; border none
for_window [title="plasma-desktop"] floating enable; border none
for_window [title="win7"] floating enable; border none
for_window [class="krunner"] floating enable; border none
for_window [class="Kmix"] floating enable; border none
for_window [class="Klipper"] floating enable; border none
for_window [class="Plasmoidviewer"] floating enable; border none
# }}} plasma #
# Carla plugins
for_window [class="Carla-bridge-lv2-gtk2"] floating enable

for_window [class="REAPER" title="FX: Track.*"] floating enable
for_window [class="REAPER" title="Mixer - *"] floating enable; border normal

for_window [class="^Crow Translate$"] floating enable

# games {{{ #
# Steam
# https://github.com/ValveSoftware/steam-for-linux/issues/1040
for_window [class="^Steam$" title="^Friends$"] floating enable
for_window [class="^Steam$" title="Steam - News"] floating enable
for_window [class="^Steam$" title=".* - Chat"] floating enable
for_window [class="^Steam$" title="^Settings$"] floating enable
for_window [class="^Steam$" title=".* - event started"] floating enable
for_window [class="^Steam$" title=".* CD key"] floating enable
for_window [class="^Steam$" title="^Steam - Self Updater$"] floating enable
for_window [class="^Steam$" title="^Steam Library Folders$"] floating enable
for_window [class="^Steam$" title="^Create or select new Steam library folder:$"] floating enable
for_window [class="^Steam$" title="^Screenshot Uploader$"] floating enable
for_window [class="^Steam$" title="^Steam Guard - Computer Authorization Required$"] floating enable
for_window [title="^Steam Keyboard$"] floating enable

assign [class="^Steam$"] $workspace_steam

for_window [title="^Rockstar Games Launcher$"] floating enable

# Move Steam games using proton to own workspace
assign [class="^steam_app_.*"] $workspace_game
# }}} games #
for_window [class="fl64.exe" title="FL Studio 20"] floating disable
for_window [class="fl64.exe"] border normal
